apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "service2.fullname" . }}
  labels:
    app: {{ include "service2.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "service2.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "service2.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
          resources: {{- toYaml .Values.resources | nindent 12 }}